#include <Arduino.h>

const int LDR_PIN = 2;
const int sensor = 4; // Pino D4 conectado ao sensor KY-037

void setup()
{
  Serial.begin(9600);
  pinMode(sensor, INPUT);
}

void loop()
{
  int ldrValue = analogRead(LDR_PIN);
  
  float voltage = ldrValue * (3.3 / 4095.0); //CONVERTE A VOLTAGEM PARA 3v3

if (ldrValue > 1200)
{
  Serial.println("Voce Perdeu 1 Moeda");
  Serial.println("---------------------------");
  delay(500);
}

  

  if (digitalRead(sensor) == HIGH)
  {
     Serial.println("RUIDO DETECTADO!!");
     Serial.println("Passa a Moeda OTARIO!");
     Serial.println("---------------------------");
    
  }

}